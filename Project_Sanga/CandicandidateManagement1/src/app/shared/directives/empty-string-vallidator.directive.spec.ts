import { EmptyStringVallidatorDirective } from './empty-string-vallidator.directive';

describe('EmptyStringVallidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyStringVallidatorDirective();
    expect(directive).toBeTruthy();
  });
});
